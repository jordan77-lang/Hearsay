/** Set true to show Canvas Translate in nav and on the landing page. */
export const SHOW_CANVAS_TRANSLATE = false;

/** Set true to show MathSay in nav and on the landing page. */
export const SHOW_MATHSAY = true;
